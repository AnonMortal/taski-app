# TaskFi dashboard — variables de build.
# Sur Cloudflare Pages : Settings → Environment variables → Production.
#
# URL du backend déployé (le front l'appelle au runtime).
VITE_API_URL=https://api.votre-domaine.com

# Optionnel : chain id EVM pour le sign-in SIWE (défaut : Base mainnet 8453).
# VITE_CHAIN_ID=8453
